pragma solidity 0.6.6;

pragma experimental ABIEncoderV2;

import "../Operations.sol";


contract MockOperations is Operations {
    // function readBlockchainProof(bytes calldata blockChainProofData) external pure returns (BlockchainProof memory bcProof) {
    //     bcProof = abi.decode(blockChainProofData, (BlockchainProof));
    // }
}
